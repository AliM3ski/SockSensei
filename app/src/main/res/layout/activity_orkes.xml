<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Gradient Background -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/lightblueg"
        android:backgroundTint="@color/open" />

    <!-- Your Main Content -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Congrats Announcement -->
        <TextView
            android:id="@+id/achievementTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="20dp"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="20dp"
            android:gravity="center"
            android:text="@string/Achievement"
            android:textColor="#000000"
            android:textSize="19sp"
            android:textStyle="bold" />

        <!-- Sock Name -->

        <!-- Sock Image -->

        <!-- Sock Description -->

        <TextView
            android:id="@+id/sockNameTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:text="@string/orkes"
            android:textColor="#000000"
            android:textSize="35sp"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/imageView"
            android:layout_width="314dp"
            android:layout_height="290dp"
            android:gravity="center"
            app:srcCompat="@drawable/okes" />

        <TextView
            android:id="@+id/sockDescriptionTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:padding="8dp"
            android:text="@string/orkes_des"
            android:textColor="#808080"
            android:textSize="16sp" />

        <!-- Reset Everything Button -->
        <Button
            android:id="@+id/resetButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:backgroundTint="#1ACAF7"
            android:text="@string/Retry" />

    </LinearLayout>
</FrameLayout>